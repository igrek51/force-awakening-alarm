package igrek.forceawaken.service.noise;

import org.junit.Test;

//import static org.assertj.core.api.Assertions.assertThat;

public class NoiseDetectorTest {
	
	@Test
	public void testCalculateAlarmVolume() {
		NoiseDetectorService noiseDetectorService = new NoiseDetectorService();
		//		assertThat(noiseDetectorService.calculateAlarmVolume(0)).isEqualTo(noiseDetectorService.noiseVolTransform[1]);
	}
	
}
